<div class="post-body">
  <%= attachment_image_tag @item, :image %>
  <p>商品名：<%= @item.name %></p>
  <p>説明：<%= @item.introduction %></p>
  <p>ジャンル：<%= @item.genre.name %></p>
  <p>税込価格（税抜価格）：<%= addTax(@item.price) %>   (<%= @item.price %>)  円</p>
  <p>販売ステータス：
        <% if @item.is_active? %>
	      <span>販売中</span>
	      <% else %>
	      <span>販売停止</span>
	      <% end %>
	</p>
  <%= link_to "編集する", edit_admin_item_path(@item) %>
</div>